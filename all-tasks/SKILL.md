---
name: all-tasks
description: 用户私有的高层任务随记，与 `workflow-text-format` 互锁、正文体例以该 Skill 为据。不供 Agent 作执行依据：除非用户在本条对话中显式 @ 本文件，否则不要读取、不要照此内容行动、 不要为「管理任务」而选本技能。
tools: ["Read", "Write"]
model: sonnet
effort: low
---
# 0. 生成规范

- **双向锁定**：（1）本文件与 `workflow-text-format/SKILL.md` 建立 **双向锁定**（路径见上 YAML 键 `paired_workflow_text_format` 与本件 `all_tasks_path`；互指用绝对路径，便于在仓库中检索）。（2）正文整体逻辑、层级与树形、对象标识**以** `workflow-text-format` 之 **`# 2`～`# 4`** **为准**。（3）**树形次级** 的增、改、加深**须**遵守其 **`# 1` / `# 2`** 中 **树形次级** 与**自检**；**若** 拟写规则与 `workflow-text-format` 存量**冲突**则**不** 擅自改对端，**须** 说明冲突并请用户**裁决** 后再改。

# 1. 使用说明

- **读者**：**自己**维护的待办/主题备忘；**非** 自动化指令。需 Agent 编辑本文件时，由用户在本对话中 **@** 本 `SKILL.md`；**未** **@** 时 Agent 勿将本件当作可执行工作流。

# 2. 主要任务

- **日程管理**：（待填。）

- **基础知识储备**：
    ├─ **RAG**：（待填。）
    └─ **Agent team**：
        ├─ Claude Code：（案例；自然语言下任务、在仓库内多步改代码/跑命令的终端与代理式工作流。）
        └─ CrewAI：（案例；Python 里以角色/crew 与任务图编排多智能体、工具与交付物分工。）

- **力量举策略开发 agent**：
    ├─ **记忆中枢**：（唯一数据仓库。原「用户状态 / 常规计划 / 事件」合并，不再分库存：历史+当前态、在跑训练与饮食计划、重要事件（伤、突破、阶段总结等）。只此处读写，免数据冲突。）
    ├─ **推理层**：（主智能体。从记忆读→做分析、RAG、计划与决策；写回记忆更新。不直聊，只思与定。）
    └─ **交互层**：（对用户。把用户话结构化交推理、结论文本人话、提问收反馈。新入口如语音 / App / 穿戴等**只**动本层，**记忆与推理**可不动。）

- **量化交易 agent**：（待填。）

# 3. 随记

- **条目**：（待填。）
